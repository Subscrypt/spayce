"use client"
import { useState } from "react"

export default function Loading() {
    const [loading, setLoading] = useState(false);
    return (
        <>
            Loading...
        </>
    )
}